import { StyleSheet } from 'react-native';
const historyGraphicStyles = StyleSheet.create({
    contentGraphic: {
        width: '100%',
        height: 220,
        backgroundColor: '#232323',
        borderRadius: 10
    }
});

export default historyGraphicStyles;